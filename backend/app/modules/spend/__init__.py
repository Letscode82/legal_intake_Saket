"""Spend & Counsel module — home of the Vendor / Sanctions Screening agent
(and, later, invoices / budgets / outside-counsel surfaces). Public surface:
agent registrations (workflow handlers) and, later, the spend router. Other
modules never import internals."""
